# UCC classification ledger — TA-anthropic_claude_enterprise

Established 2026-07-20 during Phase 0 (UCC source reconstruction). Source of truth for
what `ucc-gen build` owns versus what is hand-authored in `package/`. **Never hand-edit
anything in the generated column** — it is overwritten by the next build.

Build command:

```sh
cd apps/TA-anthropic_claude_enterprise
../../.venv/bin/ucc-gen build --source package --config globalConfig.json --ta-version <version>
# output lands in ./output/TA-anthropic_claude_enterprise/ (gitignored)
```

## Hand-authored (tracked, under `package/` + `globalConfig.json`)

| Path | Notes |
|---|---|
| `globalConfig.json` | UCC input: account/logging tabs, 4 input services, meta (version lives here) |
| `package/app.manifest` | app metadata; UCC updates version fields into output copy |
| `package/bin/{analytics_reports,compliance_activities,compliance_content,compliance_directory}.py` | modular-input scripts (scheme + dispatch); UCC generates these **only when absent** from `package/bin` |
| `package/bin/*_helper.py` (4) | thin shims re-exporting from the python package |
| `package/bin/ta_anthropic_claude_enterprise/**` | the real add-on package: api/, inputs/, adapter, checkpoint, utils |
| `package/default/inputs.conf` | scheme stanzas + python.version |
| `package/default/{props,macros,savedsearches,eventtypes,tags,collections}.conf` | search-time knowledge + KV collection |
| `package/default/app.conf` | ui label/description source (UCC merges + stamps version/build; its "can be removed" warning is accepted noise) |
| `package/default/ta-anthropic_claude_enterprise_account.conf` | deliberate placeholder so account REST endpoints exist pre-first-account |
| `package/default/data/ui/views/claude_*.xml` (5) | hand-authored dashboards |
| `package/default/data/ui/nav/default.xml` | nav (wins over UCC's generated stub) |
| `package/metadata/default.meta` | full meta incl. KO exports; UCC merge reproduces deployed file exactly |
| `package/lib/{requirements,exclude}.txt` | dependency manifest (unpinned; resolved identically to vendored lib as of 6.5.1 — pin if drift appears) |
| `package/README/README.txt`, `package/README.txt`, `package/LICENSES/`, `package/static/` | release notes, license, app icons |

## Generated by ucc-gen (never tracked, never edited)

`bin/TA-*_rh_*.py` (6 REST handlers), `bin/import_declare_test.py`, `lib/**` (pip-installed),
`appserver/**` (UCC UI incl. `static/js/build/globalConfig.json` — the **built copy**, not the source),
`default/{restmap,server,web}.conf`, `default/ta-anthropic_claude_enterprise_settings.conf`,
`default/data/ui/views/{inputs,configuration,dashboard}.xml`, `README/*.conf.spec`, `VERSION`,
`metadata/default.meta` admin stanzas (merged with package copy), `appserver/static/openapi.json`.

## Round-trip result (vs live deployed tree, 2026-07-20)

Residual diffs, all accepted:

1. `VERSION` — trailing-newline only. The doubled `1.1.0` line is **ucc-gen's own convention**
   (version + build-version), not a bug; the prior plan's "fix VERSION" hygiene item is void.
2. `default/app.conf` / `appserver/templates/base.html` — per-build epoch stamp.
3. `appserver/static/js/build/globalConfig.json` + `openapi.json` — intended fixes: version
   `1.0.1 → 1.1.0` (live built copy was stale) and activities interval default.
4. `bin/import_declare_test.py` — live copy was hand-tweaked (quote style, `dirname(__file__)`
   path insert). Generated form accepted: Splunk puts the script's own `bin/` on `sys.path`,
   so package imports work either way.
5. `bin/TA-*_rh_compliance_activities.py` — live had hand-edited default `'60'` (string);
   source standardizes on **300** matching the deployed spec and the configured input.
6. `appserver/static/customfavicon/` — empty directory in live tree; cruft, not reproduced.

## Recovered-drift decisions

- `compliance_activities` interval default: live tree disagreed with itself (spec said 300,
  built globalConfig + rh said 60). Source standardizes **300**.
- `metadata/local.meta` and `local/**` are instance state: excluded from git entirely.
