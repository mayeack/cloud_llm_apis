# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Splunk local overrides (instance-specific)
local/
local.meta

# Environment / secrets
.env
*.pem
*.key

# Presentations / docs (not part of the TA)
*.pptx

# Packaged TA build artifact (source is tracked in-repo)
TA-anthropic_claude_enterprise-*.tar.gz
