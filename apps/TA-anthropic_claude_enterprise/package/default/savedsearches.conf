# Per-TA operational alert. The governance detections moved to
# SA-ai-governance ("AI Governance - *") in v2.0.0; this one stays here because
# it watches THIS add-on's pipeline. Ships disabled — enable via
# local/savedsearches.conf.

[Anthropic Claude - TA ingestion stopped]
description = No compliance activity events have been indexed for over 2 hours - the add-on input, credentials, or network may be broken.
search = | tstats latest(_time) AS last WHERE `anthropic_claude_index` sourcetype="anthropic:compliance:activity" \
| eval lag_sec=now()-coalesce(last,0) \
| where lag_sec > 7200
cron_schedule = 25 * * * *
dispatch.earliest_time = -7d
dispatch.latest_time = now
enableSched = 1
disabled = 1
counttype = number of events
relation = greater than
quantity = 0
alert.track = 1
alert.severity = 4
alert.suppress = 1
alert.suppress.period = 4h
allow_skew = 10%
