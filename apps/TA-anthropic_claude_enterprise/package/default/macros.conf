# This TA's own index gate — used by its provider-scoped dashboards
# (claude_code_mcp, claude_ta_health) and the ingestion alert, so the TA has
# zero dependencies on SA-ai-governance. Points at the same shared index the
# SA's ai_governance_index macro uses.

[anthropic_claude_index]
definition = (index=cloud_llm_apis)
iseval = 0
