# Accelerated custom data model over the ai.* contract — the program's
# replacement for stock CIM mapping. Summary range: 7 days (raise in
# local/datamodels.conf for longer tstats lookback).

[AI_Governance]
acceleration = 1
acceleration.earliest_time = -7d
acceleration.hunk_compression = gzip
